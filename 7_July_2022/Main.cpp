#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x;
    x = 5; // assigned the value; = is getting used as the assignment operator
    // assignment operator
    if (x % 2 == 1) // 5 % 2 = 1 == 1
        cout << "It is an odd number" << endl;
    // = assignment operator
    // == comparison operator
}